{
  "name": "cofounder-studio",
  "private": true,
  "version": "0.0.1",
  "description": "Cofounder Studio (Theia-based) - thin client for the headless engine",
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "npm run build",
    "build": "npm run build:browser && npm run build:electron",
    "build:browser": "echo \"Theia browser build placeholder\"",
    "build:electron": "echo \"Theia electron build placeholder\"",
    "start": "echo \"Run Theia electron app once dependencies are installed\""
  },
  "devDependencies": {
    "@theia/cli": "^1.43.0"
  }
}
